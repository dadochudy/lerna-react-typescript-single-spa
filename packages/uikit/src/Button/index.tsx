import React from "react";
import styled from "styled-components";

const StyledButton = styled.button`
  width: 100%;
  background: red;
  color: white;
`;

const Button = () => {
  return <StyledButton>styled Butttttttton</StyledButton>;
};

export default Button;
